const sum = (arg1, arg2) => {
  console.log(arg1 + arg2);
};

const minus = (arg1, arg2) => {
  console.log(arg1 - arg2);
};

const mult = (arg1, arg2) => {
  console.log(arg1 * arg2);
};

const divide = (arg1, arg2) => {
  console.log(arg1 / arg2);
};

module.exports = {
  sum,
  minus,
  mult,
  divide,
};
